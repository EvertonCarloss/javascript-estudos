/* Variáveis = Resposáveis por guardar dados na memória.

Existem 3 métodos para se criar variaveis, ex: var, let e const. Hoje utilizamos let e const.

var e let aceitam atribuir novos valores, mas o const não aceita.

*/

// Declarando variável.
let nome = 'Everton';
let idade = 28;
let possuiFaculdade = true;

// Saída
console.log(nome, idade, possuiFaculdade);

/* E possível criar mais de uma variável sem repetir a palavra chave, utilizando a VIRGULA.

Ex:

let nome = "Everton",
    idade = 28,
    possuiFaculdade = true;

*/

/* Camel case é comum nomearmos assim: abrirModal */
